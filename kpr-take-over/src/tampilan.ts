/**
 * Sakelar tampilan — bagian yang untuk sementara disembunyikan dari UI.
 * Kodenya sengaja dipertahankan; ubah ke `true` untuk memunculkannya lagi.
 */

/** Jadwal angsuran per bulan (KPR 1 & KPR 2) di bawah tombol simpan. */
export const TAMPILKAN_JADWAL_ANGSURAN = false;

/** Kotak "Total bunga tanpa take over" di blok Bank 1. */
export const TAMPILKAN_TOTAL_BUNGA_TANPA_TAKE_OVER = false;

/** Kotak "Pokok yang dipindah" di blok Bank 2 & take over ke-2. */
export const TAMPILKAN_POKOK_PINDAH = false;

/** Sorotan "Cicilan per bulan · sebelum → sesudah" di panel hasil. */
export const TAMPILKAN_SOROTAN_CICILAN = false;
